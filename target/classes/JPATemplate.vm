package $packageName;

import jakarta.persistence.*;

import java.util.List;


@Entity
public class $class.name {
## class id part
@Id()
@GeneratedValue(strategy = GenerationType.IDENTITY)
private long id;

## attributes part
#foreach($attibute in $class.getAttributes())
private $attibute.getType().getName() $attibute.getName();
#end

## associations part
#set($counter = 0)
#foreach($ass in $class.getAssoziations())

    #if ($ass.getInverse().getMultiplicity() == "0..*" && ($counter%2 == 0))
    @OneToMany(mappedBy = "$ass.getRoleName()")
    private List<$ass.getInverse().getReference().getName()> $ass.getInverse().getName();

    #elseif($ass.getInverse().getMultiplicity() == "1" && ($counter%2 == 0))
    @ManyToOne
    private $ass.getInverse().getReference().getName() $ass.getInverse().getRoleName();

    #elseif($ass.getInverse().getMultiplicity() == "0..1" && ($counter%2 == 0))
    @ManyToOne
    private $ass.getInverse().getReference().getName() $ass.getInverse().getRoleName();

    #end

    #set($counter = $counter + 1)
#end


public $class.getName() () {}



## constructor part
#set($counter = 1)
public $class.getName() (
        ################################## eigene Attribute
            #foreach($attr in $class.getAttributes())
                $attr.getType() $attr.getName()
                   #if($class.getAttributes().size() > $counter)
                           , #set ($counter = $counter + 1)
                    #end
            #end
            ####################### obligatorische bezeihungsattribute
            #foreach($ass in $class.getAssoziations())
                #if($ass.getInverse().getMultiplicity() == "1")
                    , $ass.getInverse().getReference().getName() $ass.getInverse().getName()
                #end
            #end
            ) {
            ########################### eigene Attribute
            #foreach($attr in $class.getAttributes())
                this.$attr.getName() = $attr.getName();
            #end
            ######################### obligatorische bezeihungsattribute
            #foreach($ass in $class.getAssoziations())
                #if($ass.getInverse().getMultiplicity() == "1")
                    this.$ass.getInverse().getName() = $ass.getInverse().getName();
                #end
            #end
        }



## setters part
#foreach($attibute in $class.getAttributes())
public void set$attibute.getName()($attibute.getType() var){
        this.$attibute.getName() =var;
        }
#end

## getters part
#foreach($attibute in $class.getAttributes())
public $attibute.getType() get$attibute.getName()() {
        return this.$attibute.getName();
        }
#end



        }
